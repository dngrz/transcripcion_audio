# =====================================================================
# Variables de entorno para Podman/Docker Compose.
# Copiar este archivo a .env y ajustar los valores.
#   cp .env.example .env
# =====================================================================

# ---------------------------------------------------------------------
# Base de datos PostgreSQL
# ---------------------------------------------------------------------
POSTGRES_DB=transcripcion_gestion
POSTGRES_USER=database_username
POSTGRES_PASSWORD=database_password
POSTGRES_PORT=5432

# ---------------------------------------------------------------------
# Perfil de Spring Boot (local, dev, test, prod)
# ---------------------------------------------------------------------
SPRING_PROFILES_ACTIVE=prod

# ---------------------------------------------------------------------
# Puertos publicados en el host
# ---------------------------------------------------------------------
BACKEND_PORT=8080
FRONTEND_PORT=4200

# ---------------------------------------------------------------------
# Seguridad / JWT
# JWT_SECRET debe tener al menos 32 caracteres (256 bits) para HS256.
# ---------------------------------------------------------------------
JWT_SECRET=cambia-este-secreto-por-uno-largo-y-aleatorio-de-32-o-mas-caracteres
JWT_EXPIRATION_MS=86400000
TOKEN_VALIDATION_ENABLED=true

# ---------------------------------------------------------------------
# OpenAI
# ---------------------------------------------------------------------
OPENAI_API_KEY=sk-reemplaza-por-tu-api-key
OPENAI_BASE_URL=https://api.openai.com/v1/
OPENAI_MODEL=gpt-transcribe
OPENAI_LANGUAGE=es

# ---------------------------------------------------------------------
# CORS (solo necesario si el frontend llama al backend desde otro origen)
# ---------------------------------------------------------------------
CORS_ALLOWED_ORIGINS=http://localhost:4200

# ---------------------------------------------------------------------
# Usuario administrador inicial (se crea si no existe)
# ---------------------------------------------------------------------
ADMIN_EMAIL=admin@transcripcion.local
ADMIN_PASSWORD=Admin123*
