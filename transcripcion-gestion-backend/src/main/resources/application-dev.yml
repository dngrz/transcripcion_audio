spring:
  datasource:
    url: jdbc:postgresql://${DB_HOST:db}:${DB_PORT:5432}/${DB_NAME:transcripcion_gestion}
    username: ${DB_USER:transcripcion}
    password: ${DB_PASSWORD:transcripcion}
    driver-class-name: org.postgresql.Driver

security:
  token-validation:
    enabled: true

logging:
  level:
    com.transcripcion.gestion: DEBUG
